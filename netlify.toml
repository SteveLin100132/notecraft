[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Static deploy only — no functions, no runtime API.
# Pagefind index is generated by `npm run build` (astro build && pagefind --site dist).
