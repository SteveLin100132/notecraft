---
title: Waterfall V.S Agile
description: ''
tags:
  - AI 陪跑筆記
  - 專案管理
series: 專案管理基礎
order: 2
createdAt: '2026-06-12'
updatedAt: '2026-06-12'
---

# SDLC - Waterfall V.S Agile

前面的筆記我們認識了專案與產品的差異，接下來我們來看看在專案管理中，兩種常見的執行方法：**Waterfall(瀑布式)** 與 **Agile(敏捷式)**。

這類的執行方法我們稱之為 [SDLC](https://aws.amazon.com/tw/what-is/sdlc/)（Software Development Life Cycle，軟體開發生命週期）模型，雖然名字裡有「軟體開發」，但其實不只軟體專案，其他類型的專案也常借用這兩種方法論來規劃執行。

SDLC 簡單來說就是指的是從構思到退役所經歷的完整流程，將執行工作拆解成數個明確的階段，協助團隊有系統地規劃、執行與管控專案。

目前先針對常見的 Waterfall 與 Agile 做比較，其他模式我也沒接觸過 😅，所以暫時不展開。  

## Waterfall(瀑布式)

階段清楚、文件齊全、前期規劃重、變更成本高。
適合需求穩定、合規要求高、或對交付物有明確規範的專案(例如:政府標案、ERP 導入、金融系統)。

## Agile(敏捷式)

迭代交付、擁抱變化、強調溝通與反饋。
適合需求不明確、需要快速試錯、市場變化快的產品(例如:消費型 App、SaaS 產品)。

{/* @ai-visualize
id: waterfall-agile-dimensions
type: motion
prompt: |
  瀑布 vs 敏捷的多維度差異,做成可逐項切換的互動走查元件,放在文章開頭作為全局概覽。
  核心洞察:一個把「變更」視為威脅而力求凍結(剛性),一個把變更視為原料而擁抱流動(柔性)。
  互動:頂部一排面向選擇器(可點擊)+ 底部步進控制(上/下一個、進度點),
  讀者每切換一個面向,就並排看到瀑布(左、冷藍、方角、Lock 系語意)與
  敏捷(右、暖橘、圓角、Repeat 系語意)在該面向上的對照。
  面向包含:交付方式、需求變更、文件、規劃時機、溝通反饋、適用情境。
  每個面向下方加一條「剛性 — 柔性」漸層對比軸,
  瀑布(方點)落在剛性端、敏捷(圓點)落在柔性端,切換時以動畫滑動到位,
  把「同一面向上兩種方法的拉扯」具象化。
  以 react state + motion 驅動切換動畫(200–400ms ease-out,
  並用 useReducedMotion 尊重 prefers-reduced-motion)。
  禁止 emoji,語意一律用 lucide-react icon;色票、字級、間距遵循 trendlink-design,
  方角/暖色=剛性瀑布、圓角/冷暖對比=柔性敏捷。
status: generated
*/}

import GeneratedFrame from "@/components/GeneratedFrame.astro";
import WaterfallAgileDimensions from '@/components/generated/waterfall-agile-dimensions'

<GeneratedFrame id="waterfall-agile-dimensions" type="motion" prompt={"瀑布 vs 敏捷的多維度差異,做成可逐項切換的互動走查元件,放在文章開頭作為全局概覽。\n核心洞察:一個把「變更」視為威脅而力求凍結(剛性),一個把變更視為原料而擁抱流動(柔性)。\n互動:頂部一排面向選擇器(可點擊)+ 底部步進控制(上/下一個、進度點),\n讀者每切換一個面向,就並排看到瀑布(左、冷藍、方角、Lock 系語意)與\n敏捷(右、暖橘、圓角、Repeat 系語意)在該面向上的對照。\n面向包含:交付方式、需求變更、文件、規劃時機、溝通反饋、適用情境。\n每個面向下方加一條「剛性 — 柔性」漸層對比軸,\n瀑布(方點)落在剛性端、敏捷(圓點)落在柔性端,切換時以動畫滑動到位,\n把「同一面向上兩種方法的拉扯」具象化。\n以 react state + motion 驅動切換動畫(200–400ms ease-out,\n並用 useReducedMotion 尊重 prefers-reduced-motion)。\n禁止 emoji,語意一律用 lucide-react icon;色票、字級、間距遵循 trendlink-design,\n方角/暖色=剛性瀑布、圓角/冷暖對比=柔性敏捷。"}>
  <WaterfallAgileDimensions client:visible />
</GeneratedFrame>

完整對照如下表，方便一次掃讀全部面向：

| 面向 | Waterfall 瀑布 | Agile 敏捷 |
| --- | --- | --- |
| 交付方式 | 階段一次性交付 | 迭代增量交付 |
| 需求變更 | 成本高、抗拒變更 | 成本低、擁抱變化 |
| 文件 | 前期齊全、文件驅動 | 精簡夠用即可 |
| 規劃時機 | 前期規劃重、一次定案 | 持續滾動、隨迭代調整 |
| 溝通反饋 | 里程碑階段審查 | 高頻、持續反饋 |
| 適用情境 | 需求穩定 / 合規（政府標案、ERP 導入、金融系統） | 需求不明 / 快速試錯（消費型 App、SaaS 產品） |

{/* @ai-visualize
id: waterfall-agile-change-response
type: motion
prompt: |
  互動式對比「需求中途變更」時,瀑布與敏捷的反應差異 —— 這是本篇的核心張力,
  目標是讓讀者「親身體驗」同一個變更在兩種方法下代價天差地別。

  版面上下兩條泳道,共用同一條時間軸(專案進度 0% → 100%):
  - 上排「瀑布」:一條線性階段鏈(需求 → 設計 → 開發 → 測試 → 上線),
    階段之間以實線箭頭單向串接。
  - 下排「敏捷」:一串重複的小迭代迴圈(Sprint),以循環箭頭連續推進。

  互動:讀者拖動時間軸 slider 推進專案進度,並可在任意點按下「需求變更」按鈕。
  - 觸發後,瀑布泳道:在已完成階段出現一道紅色裂痕,需要倒回前面階段重做,
    一條「變更成本」量條大幅上升並標示「變更成本高」。
  - 敏捷泳道:變更只是被收進「下一個 Sprint」的待辦,成本條僅微幅變動,
    標示「擁抱變化」。
  收斂訊息:在專案越後期觸發變更,瀑布的成本攀升越劇烈,敏捷則相對平緩。

  底層幾何(泳道、階段方塊、Sprint 迴圈、成本條)用手寫 SVG 繪製,
  以 react state + motion 驅動推進與變更動畫(200–400ms ease-out,
  並用 useReducedMotion 尊重 prefers-reduced-motion)。
  禁止 emoji,狀態 / 方向語意一律用 lucide-react icon。
  色彩、字級、間距遵循 trendlink-design。
status: generated
*/}

import WaterfallAgileChangeResponse from '@/components/generated/waterfall-agile-change-response'

<GeneratedFrame id="waterfall-agile-change-response" type="motion" prompt={"互動式對比「需求中途變更」時,瀑布與敏捷的反應差異 —— 這是本篇的核心張力,\n目標是讓讀者「親身體驗」同一個變更在兩種方法下代價天差地別。\n\n版面上下兩條泳道,共用同一條時間軸(專案進度 0% → 100%):\n- 上排「瀑布」:一條線性階段鏈(需求 → 設計 → 開發 → 測試 → 上線),\n  階段之間以實線箭頭單向串接。\n- 下排「敏捷」:一串重複的小迭代迴圈(Sprint),以循環箭頭連續推進。\n\n互動:讀者拖動時間軸 slider 推進專案進度,並可在任意點按下「需求變更」按鈕。\n- 觸發後,瀑布泳道:在已完成階段出現一道紅色裂痕,需要倒回前面階段重做,\n  一條「變更成本」量條大幅上升並標示「變更成本高」。\n- 敏捷泳道:變更只是被收進「下一個 Sprint」的待辦,成本條僅微幅變動,\n  標示「擁抱變化」。\n收斂訊息:在專案越後期觸發變更,瀑布的成本攀升越劇烈,敏捷則相對平緩。\n\n底層幾何(泳道、階段方塊、Sprint 迴圈、成本條)用手寫 SVG 繪製,\n以 react state + motion 驅動推進與變更動畫(200–400ms ease-out,\n並用 useReducedMotion 尊重 prefers-reduced-motion)。\n禁止 emoji,狀態 / 方向語意一律用 lucide-react icon。\n色彩、字級、間距遵循 trendlink-design。"}>
  <WaterfallAgileChangeResponse client:visible />
</GeneratedFrame>

## 我的觀察

實務上很少有「純瀑布」或「純敏捷」,多數團隊會視專案性質做混搭。
例如:對外契約用瀑布的方式定義範疇與里程碑,對內團隊則用敏捷的方式跑 Sprint 來推進。

{/* @ai-visualize
id: waterfall-agile-hybrid
type: diagram
prompt: |
  呈現實務上的「混搭模型」,把我的觀察視覺化:同一個專案,
  對外用瀑布定範疇與里程碑、對內用敏捷跑 Sprint,兩者並存而非二選一。

  以「剛性外框 + 柔性內核」的隱喻構圖:
  - 外層「對外契約層(瀑布)」:一條水平時間軸,上面放幾個固定的里程碑節點
    (方形、實線、剛性),標注範疇定義與交付里程碑。
  - 內層「對內團隊層(敏捷)」:在每兩個相鄰里程碑之間,以循環箭頭跑 Sprint,
    用圓形迭代迴圈(柔性)表現持續推進,把成果餵向下一個里程碑。

  視覺上讓外層直線 / 方塊 / 實線 與 內層圓形 / 循環 形成明確對比。
  手寫 SVG 為主;若加上 motion 讓內層 Sprint 迴圈緩慢轉動 / 逐步推進更佳
  (節制動畫,並尊重 prefers-reduced-motion)。
  禁止 emoji,需要圖示時用 lucide-react;色票與間距遵循 trendlink-design。
status: generated
*/}

import WaterfallAgileHybrid from '@/components/generated/waterfall-agile-hybrid'

<GeneratedFrame id="waterfall-agile-hybrid" type="diagram" prompt={"呈現實務上的「混搭模型」,把我的觀察視覺化:同一個專案,\n對外用瀑布定範疇與里程碑、對內用敏捷跑 Sprint,兩者並存而非二選一。\n\n以「剛性外框 + 柔性內核」的隱喻構圖:\n- 外層「對外契約層(瀑布)」:一條水平時間軸,上面放幾個固定的里程碑節點\n  (方形、實線、剛性),標注範疇定義與交付里程碑。\n- 內層「對內團隊層(敏捷)」:在每兩個相鄰里程碑之間,以循環箭頭跑 Sprint,\n  用圓形迭代迴圈(柔性)表現持續推進,把成果餵向下一個里程碑。\n\n視覺上讓外層直線 / 方塊 / 實線 與 內層圓形 / 循環 形成明確對比。\n手寫 SVG 為主;若加上 motion 讓內層 Sprint 迴圈緩慢轉動 / 逐步推進更佳\n(節制動畫,並尊重 prefers-reduced-motion)。\n禁止 emoji,需要圖示時用 lucide-react;色票與間距遵循 trendlink-design。"}>
  <WaterfallAgileHybrid client:visible />
</GeneratedFrame>